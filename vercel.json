{
  "framework": "nextjs",
  "buildCommand": "node scripts/switch-db-provider.js $DATABASE_PROVIDER && npx prisma db push --accept-data-loss && npx prisma generate && next build",
  "installCommand": "bun install",
  "outputDirectory": ".next"
}
