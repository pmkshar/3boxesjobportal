{
  "framework": "nextjs",
  "buildCommand": "node scripts/switch-db-provider.js $DATABASE_PROVIDER && npx prisma generate && next build",
  "installCommand": "bun install",
  "outputDirectory": ".next"
}
